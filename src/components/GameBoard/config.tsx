
export const getColor = (i) => {
  return i % 2 == 1 ? `black` : `red`;
};

export const tileHeight = 100;
export const tileWidth = 100;
export const boardHeight = tileHeight * 3;
export const BOTTOM_ROW_HEIGHT = 100;
